# 0.4.0開発ブランチの検証記録

[English](validation.md) | [日本語](validation_ja.md)

2026-09-05にmacOSで実施したローカル検証です。

| 検証 | 結果 |
| --- | --- |
| 全テスト、Python 3.11.14 | 66件成功 |
| 全テスト、Python 3.13.11 | 66件成功 |
| 全テスト、Python 3.14.2 | 66件成功 |
| 保守対象ソースのRuff検査 | 成功 |
| `d90beee330bca581d0a52eddec38362c3b28d50a`との従来出力の比較 | 7種類のテスト素材でバイト単位の一致 |
| ソース配布物とwheelのビルド | 成功 |
| 新規Python 3.12.12環境へのwheelインストール | バージョン表示、複数言語のindex、pack、check、従来のJava buildが成功 |

比較に使った7種類は`sample.py`、`sample.java`、`java8_syntax.java`、`large_file.py`、`function_only.py`、`empty.py`、`comments_only.py`です。両版で同じ入力パスを使い、`INDEX.md`、`MAP.json`、`parts/`を含むすべての生成ファイルをバイト単位で比較しました。

全66件の内訳は既存30件と共通エンジン36件です。追加分には原文の完全復元、Unicode・CRLF、複数言語、外側の分岐条件と例外領域の参照、未解決・曖昧な呼出候補、独自アダプターとカウンター、oversized／opaque、文脈省略記録の整合性、CLI終了コードを含みます。

ロックファイルは既存の依存バージョン（Tree-sitter 0.25.2、Java文法0.23.5）を維持しています。新規wheel環境ではTree-sitter 0.26.0とJava文法0.23.5も確認しました。すべての依存バージョンの組合せを検証したという意味ではありません。

CIはLinux・Windows・macOSとPython 3.11・3.13・3.14の組合せで設定しています。ホスト側のCI実行結果はこのローカル検証とは別です。これらのテストは素材に対する構造と出力の契約を確認するものであり、Oracle／Java／Pythonの完全な意味解析や、実運用でのLLM精度向上を証明するものではありません。

## 文書の検証

2026-09-05の文書更新で、ルート／共通エンジンの現行Markdown文書20件と、ローカルリンク・見出し参照111件を確認しました。ルートREADME、build仕様書、共通エンジン5文書の計7組について、日英の実行例の一致と言語切替の導線を確認しています。過去リリースのサンプルと外部サイトは、このローカルリンク検査の対象外です。

index／tree／pack／check／showのCLI操作、Python API、独自アダプター、従来の本文チェックサム計算、oversizedの終了コード3とその後の整合性検査成功を実行で確認しました。ソース配布物には共通エンジン文書10件と両言語のbuild仕様書が含まれることを確認しています。文書変更後も全66件のテストとRuffが通りました。この追加検証は、意味解析やLLM品質について新たな保証を加えるものではありません。
