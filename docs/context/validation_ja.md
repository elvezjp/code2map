# 検証結果

[English](validation.md) | [日本語](validation_ja.md)

この文書は、現時点で確認できている最新の検証結果をまとめたものです。再検証時は該当する結果を上書き更新し、時系列の履歴は追記しません。検証ごとに実施日を残し、異なる実行時点の結果を区別します。

## 検証結果の概要

| 検証 | 最新の実施日 | 結果 |
| --- | --- | --- |
| ローカルの全テスト、macOS / Python 3.14.2（`b5321f3`・`d3ff096`） | 2026-09-09 | 80件成功（C#アダプター12件を含む） |
| Ruff（`uv run ruff check .`） | 2026-09-09 | 成功（ロック版0.16.6と最新版の両方） |
| GitHub Actions CI、Linux・Windows・macOS × Python 3.11・3.13・3.14 | 2026-09-09、`d3ff096`（C#アダプターのPR） | 全9ジョブ成功 |
| 約4万行のPL/SQLパッケージのpacket境界 | 2026-09-07 | 全137packetで行の途中の境界なし |
| `d90beee330bca581d0a52eddec38362c3b28d50a`との従来出力の比較 | 2026-09-05 | 7種類のテスト素材でバイト単位の一致 |
| v0.3.0のサンプルとの`build`出力の比較 | 2026-09-07 | `parts/`の`original:`行を除きバイト単位の一致 |
| ソース配布物とwheelのビルド | 2026-09-05 | 成功 |
| 新規Python 3.12.12環境へのwheelインストール | 2026-09-05 | バージョン表示、複数言語のindex、pack、check、従来のJava buildが成功 |
| 文書リンク、日英の実行例、配布物の収録内容 | 2026-09-05 | 成功。対象範囲は後述 |

## テスト範囲と出力互換性

全80件の内訳は従来機能30件と共通エンジン50件です。原文の完全復元、Unicode・CRLF、複数言語、外側の分岐条件と例外領域の参照、未解決・曖昧な呼出候補、独自アダプターとカウンター、oversized／opaque、文脈省略記録の整合性、CLI終了コードを含みます。

packet境界については、全packetの境界が行頭・行末にあることを検査する性質テストと、生の範囲は予算に収まるが行揃え後は収まらない子の回帰テストで確認しています。約4万行のPL/SQLパッケージ（予算40,000／予約4,000バイト）でも、全137packetで行の途中の境界がないことを確認しました。行揃えの修正（[#27](https://github.com/elvezjp/code2map/issues/27)）によるpacket数とoversized数の変化はありませんでした。

比較に使った7種類は`sample.py`、`sample.java`、`java8_syntax.java`、`large_file.py`、`function_only.py`、`empty.py`、`comments_only.py`です。両版で同じ入力パスを使い、`INDEX.md`、`MAP.json`、`parts/`を含むすべての生成ファイルをバイト単位で比較しました。

共通エンジンの出力サンプルは、`docs/examples/v0.4.0/`の各言語の`context/`に収録しています。

C#アダプター（[#36](https://github.com/elvezjp/code2map/issues/36)）は12件の回帰テストで確認しています。CRLF・Unicode・オーバーロードでの索引の再現性、分割後の`else`条件・ループヘッダー・例外ハンドラーの保持、switch文と式形式メンバー、150armのswitch式の分割、分割不能な`do`、構文エラーの`opaque`化、ファイルスコープ名前空間・`#region`・ローカル関数・`goto`、クラスと名前空間を囲む`#if`の内部構造の抽出と分割後の条件ヘッダーの保持、共有する`case`ラベルの分岐ヘッダーへの保持、別ファイルの`partial`型、4言語の混在ディレクトリです。合成サンプルは`docs/examples/v0.5.0/csharp/`に収録しています（予算6,000バイトで8packet・ready 8・oversized 0・`check` passed）。既存のPython・Java・PL/SQLサンプルは再生成しても構造と分割が同一で、変わるのは実行環境の記録だけです。

## 依存関係と検証の限界

現在のロックファイルはTree-sitter 0.26.0、Java文法0.23.5、C#文法0.23.5です（C#アダプター追加時点）。それ以前の検証記録（2026-09-05〜08）はTree-sitter 0.25.2・Java文法0.23.5のロックで行い、新規wheel環境ではTree-sitter 0.26.0とJava文法0.23.5も確認しました。すべての依存バージョンの組合せを検証したという意味ではありません。

概要表のCI結果はホスト側での実行結果で、ローカルの全テスト結果はmacOSでの実行結果です。これらのテストは素材に対する構造と出力の契約を確認するものであり、Oracle／Java／Pythonの完全な意味解析や、実運用でのLLM精度向上を証明するものではありません。

## 文書の検証

2026-09-05の文書検証で、ルート／共通エンジンの現行Markdown文書20件と、ローカルリンク・見出し参照111件を確認しました。ルートREADME、build仕様書、共通エンジン5文書の計7組について、日英の実行例の一致と言語切替の導線を確認しています。過去リリースのサンプルと外部サイトは、このローカルリンク検査の対象外です。

index／tree／pack／check／showのCLI操作、Python API、独自アダプター、従来の本文チェックサム計算、oversizedの終了コード3とその後の整合性検査成功を実行で確認しました。ソース配布物には共通エンジン文書10件と両言語のbuild仕様書が含まれることを確認しています。
